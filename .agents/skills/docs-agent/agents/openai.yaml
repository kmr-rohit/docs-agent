interface:
  display_name: "Docs Agent"
  short_description: "Repo-specific patterns and workflows for docs-agent"
  default_prompt: "Use the docs-agent repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true