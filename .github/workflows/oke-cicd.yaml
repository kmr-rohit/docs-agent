name: Build, Test, and Deploy to OKE

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: mcp-kubeflow-docs
  K8S_NAMESPACE: docs-agent
  OKE_CLUSTER_OCID: ${{ secrets.OKE_CLUSTER_OCID }}

jobs:
  test-and-compile:
    name: Compile and Test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r kagent-feast-mcp/mcp-server/requirements.txt
          pip install -r requirements-test.txt
          pip install ruff

      - name: Ruff lint
        run: ruff check kagent-feast-mcp/mcp-server tests pipelines

      - name: Compile MCP server
        run: python -m py_compile kagent-feast-mcp/mcp-server/*.py

      - name: Run Unit Tests
        run: pytest -v --tb=short

  build-and-deploy:
    name: Build, Push, and Deploy to OKE
    needs: test-and-compile
    if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set Image Tag
        run: echo "tag=${GITHUB_SHA::7}" >> "$GITHUB_ENV"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.GHCR_USERNAME }}
          password: ${{ secrets.GHCR_TOKEN }}

      - name: Build and Push MCP Image
        uses: docker/build-push-action@v5
        with:
          context: ./kagent-feast-mcp/mcp-server
          file: ./kagent-feast-mcp/mcp-server/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ secrets.GHCR_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.tag }}
            ${{ env.REGISTRY }}/${{ secrets.GHCR_USERNAME }}/${{ env.IMAGE_NAME }}:latest

      - name: Install OCI CLI
        uses: oracle-actions/setup-oci-cli@v1.2.0
        with:
          oci-user: ${{ secrets.OCI_USER_OCID }}
          oci-tenancy: ${{ secrets.OCI_TENANCY_OCID }}
          oci-region: ${{ secrets.OCI_REGION }}
          oci-key-file: ${{ secrets.OCI_KEY_FILE }}
          oci-fingerprint: ${{ secrets.OCI_FINGERPRINT }}

      - name: Generate Kubeconfig for OKE
        run: |
          mkdir -p ~/.kube
          oci ce cluster create-kubeconfig \
            --cluster-id ${{ env.OKE_CLUSTER_OCID }} \
            --file ~/.kube/config \
            --region ${{ secrets.OCI_REGION }} \
            --token-version 2.0.0
          chmod 600 ~/.kube/config

      - name: Deploy to OKE
        env:
          FULL_IMAGE: ${{ env.REGISTRY }}/${{ secrets.GHCR_USERNAME }}/${{ env.IMAGE_NAME }}:${{ env.tag }}
        run: |
          set -euo pipefail

          kubectl create namespace "${{ env.K8S_NAMESPACE }}" --dry-run=client -o yaml | kubectl apply -f -

          kubectl create secret docker-registry ghcrsecret \
            --namespace "${{ env.K8S_NAMESPACE }}" \
            --docker-server="${{ env.REGISTRY }}" \
            --docker-username="${{ secrets.GHCR_USERNAME }}" \
            --docker-password="${{ secrets.GHCR_TOKEN }}" \
            --docker-email="${{ secrets.GHCR_USERNAME }}@users.noreply.github.com" \
            --dry-run=client -o yaml | kubectl apply -f -

          kubectl apply -f kagent-feast-mcp/manifests/mcp-server/mcp-server.yaml
          kubectl set image deployment/mcp-kubeflow-docs \
            mcp-server="${FULL_IMAGE}" \
            -n "${{ env.K8S_NAMESPACE }}" || true
          kubectl patch deployment/mcp-kubeflow-docs -n "${{ env.K8S_NAMESPACE }}" --type=merge \
            -p '{"spec":{"template":{"spec":{"imagePullSecrets":[{"name":"ghcrsecret"}]}}}}'
          kubectl rollout status deployment/mcp-kubeflow-docs \
            -n "${{ env.K8S_NAMESPACE }}" --timeout=600s

          kubectl apply -f manifests/qwen-llm-service.yaml
          kubectl apply -f kagent-feast-mcp/manifests/kagent/setup.yaml
          kubectl apply -f manifests/serving-runtime.yaml
          kubectl apply -f manifests/inference-service.yaml
          kubectl annotate inferenceservice qwen -n "${{ env.K8S_NAMESPACE }}" serving.kserve.io/stop- --overwrite 2>/dev/null || true

      - name: Smoke test MCP + KServe
        run: |
          set -euo pipefail
          kubectl wait --for=condition=Ready inferenceservice/qwen \
            -n "${{ env.K8S_NAMESPACE }}" --timeout=1800s || echo "KServe not ready yet; check cluster manually."

          POD="$(kubectl get pods -n "${{ env.K8S_NAMESPACE }}" -l serving.kserve.io/inferenceservice=qwen -o jsonpath='{.items[0].metadata.name}')"
          kubectl exec -n "${{ env.K8S_NAMESPACE }}" "${POD}" -c kserve-container -- python3 -c "
          import urllib.request, json
          p=json.dumps({'model':'qwen2.5-3b-instruct','messages':[{'role':'user','content':'ping'}],'max_tokens':8}).encode()
          r=urllib.request.urlopen(urllib.request.Request('http://127.0.0.1:8080/openai/v1/chat/completions',data=p,headers={'Content-Type':'application/json'}), timeout=60)
          print('kserve ok', r.status)
          "

          kubectl get agent,modelconfig -n "${{ env.K8S_NAMESPACE }}"
